import React from 'react';

const PadsBoardControl = ({ children }) => {
    return (
        <div className='pads-board-control'>
            {children}
        </div>
    );
};

export default PadsBoardControl;